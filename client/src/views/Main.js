import React from 'react';
import Detail from './Detail';

const Main = () => {
    return (
        <Container style={{display: "flex"}}>
            <Detail/>    
        </Container>
    );
}

export default Main;
